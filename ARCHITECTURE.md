# معماری سیستم و راهنمای توسعه (System Architecture & Agent Guide)

این سند مرجع جامع معماری فنی، جریان داده، تفکیک ماژول‌ها و استانداردهای توسعه پروژه **RTL Markdown Studio** برای راهنمایی عامل هوش مصنوعی (AI Agent) و توسعه‌دهندگان است.

---

## ۱. اصول و پارادایم‌های بنیادین (Core Principles)

1. **تفکیک دغدغه‌ها و تک‌مسئولیتی (SoC & SRP):** هر فایل یا کامپوننت منحصراً یک وظیفه واضح دارد.
2. **سقف خطوط فایل (File Size Limit):** هیچ فایلی نباید بیشتر از ۲۰۰ تا ۳۰۰ خط کد باشد. در صورت گسترش منطق، باید به زیرکامپوننت یا هوک اختصاصی تفکیک شود.
3. **حریم خصوصی بدون سرور (Zero-Backend Privacy - ADR-0008):** کلیه پردازش‌های متنی، آمارگیری و رندر در حافظه رم مرورگر کلاینت انجام می‌شود و هیچ داده‌ای به خارج ارسال نمی‌شود.
4. **خط لوله واکنشی معوق (Deferred Reactive Pipeline - ADR-0010):** پردازش اسناد سنگین مارک‌داون با `useDeferredValue` ایزوله شده تا پاسخ‌دهی تایپ در ادیتور همیشه ۶۰fps بماند.
5. **درز عمیق دامنه (Deep Domain Seam - ADR-0005):** هسته پردازش مارک‌داون در `services/markdownEngine.ts` و توابع کمکی `utils/` توابع خالص (Pure Functions) بدون هیچ‌گونه وابستگی به React یا DOM هستند.
6. **مستندسازی استاندارد (Standard JSDoc):** تمام توابع، هوک‌ها، تایپ‌ها و اینترفیس‌های کلیدی باید دارای توضیحات شفاف JSDoc باشند.
7. **سیستم طراحی اختصاصی (Design System):** انطباق کامل با توکن‌های بصری `DESIGN.md`، عدم استفاده از گرادیان‌های کلیشه‌ای، رعایت تم دوگانه و پالت رنگی Zinc/Emerald.

---

## ۲. نمای کلی جریان داده (Data Flow Pipeline)

```
[ ورودی کاربر / تایپ در ادیتور / آپلود فایل ]
                     │
                     ▼
           [ store/MarkdownContext ]
      ├── rawMarkdown (حالت زنده برای ادیتور)
      └── deferredRawMarkdown (useDeferredValue)
                     │
                     ▼
       [ services/markdownEngine.ts ]
      ├── utils/rtlConverter (ایزولاسیون کدها و راست‌چین‌سازی جداول)
      ├── utils/statsCalculator (کلمات، کاراکترها، خطوط، زمان مطالعه)
      └── استخراج هوشمند عنوان سند (Setext / ATX Title)
                     │
                     ▼
   [ ProcessedDocument { rtlMarkdown, stats, title } ]
                     │
                     ▼
         [ components/views/preview-view ]
      ├── ReactMarkdown + remark-gfm
      ├── کامپوننت‌های کاستوم کد (CodeBlock + Highlight.js)
      └── جداول واکنش‌گرای راست‌چین (MarkdownTable)
```

---

## ۳. نقشه‌ی ساختار دایرکتوری‌ها و تفکیک ماژولار

```
/
├── app/                      # روت و قالب Next.js 15 App Router
│   ├── layout.tsx            # چیدمان ریشه و ارائه‌دهنده‌های زمینه
│   ├── page.tsx              # صفحه اصلی و اسمبل ویوها
│   └── globals.css           # استایل‌های سراسری با Tailwind CSS v4
├── services/                 # ماژول عمیق دامنه پردازش سند
│   └── markdownEngine.ts     # اینترفیس واحد تحلیل، پردازش و خروجی مارک‌داون
├── store/                    # مدیریت وضعیت متمرکز
│   └── MarkdownContext.tsx   # کانتکست سراسری، خط‌لوله معوق و توست‌ها
├── components/               # کامپوننت‌های تفکیک‌شده رابط کاربری
│   ├── layout/               # ساختار پوسته (Header, StudioDock, FocusModeBar, SmoothScroll)
│   ├── views/                # ویوهای اصلی (EditorView, PreviewView, Modals, StatsBadges)
│   ├── ui/                   # المان‌های پایه دیزاین‌سیستم (Button, Modal, CodeBlock, Dock, Table)
│   ├── motion/               # المان‌های حرکتی تخصصی (ActionSwap, Loader, ThemeToggle)
│   └── providers/            # ارائه‌دهنده‌های بافتاری موشن و تم
├── hooks/                    # هوک‌های کلاینت با وظایف مشخص
│   ├── useActiveLinePosition # محاسبه ریاضی موقعیت خط فعال ادیتور (ADR-0009)
│   ├── useClipboard.ts       # عملیات کپی امن با فیدبک
│   ├── useDecodeText.ts      # افکت رمزگشایی متنی کاراکترها
│   ├── useDesktopSmoothScroll# راه‌اندازی شرطی Lenis فقط در دسکتاپ (ADR-0002)
│   └── useStudioDockActions  # اتصال داک ابزار به کانتکست برنامه
├── utils/                    # توابع کمکی محاسباتی و بدون عوارض جانبی
│   ├── rtlConverter.ts       # هسته تبدیل متون به راست‌چین و نگهداری کدهای LTR
│   ├── statsCalculator.ts    # محاسبات آماری سند و کاراکترهای فارسی
│   ├── syntaxHighlighter.ts  # هایلایت نحوی بلاک‌ها با Highlight.js
│   ├── persianizer.ts        # تبدیل ارقام به اعداد استاندارد فارسی
│   ├── fileHelpers.ts        # هندلینگ فایل، درگ‌واند‌دراپ و دانلود امن
│   └── deviceDetector.ts     # تشخیص قابلیت‌های پوینتر و نوع دستگاه
├── types/                    # تعاریف داده و اینترفیس‌های Type-Safe
│   ├── markdown.ts           # تایپ‌های موتور پردازش و کانتکست
│   ├── ui.ts                 # تایپ‌های تم، حالت‌های نمایش و نماها
│   ├── i18n.ts               # اینترفیس‌های فرهنگ لغت دوزبانه
│   └── dock.ts               # ساختار آیتم‌ها و رویدادهای داک استودیو
├── constants/                # مقادیر ایستا و دیکشنری‌ها
│   ├── domain.ts             # ثوابت زمانی، ابعاد و مقادیر پیش‌فرض
│   ├── translations.ts       # دیکشنری محلی دوزبانه (fa / en)
│   └── sampleMarkdown.ts     # نمونه‌های متنی آزمایشی
├── adr/                      # سوابق تصمیمات معماری (ADR-0001 تا ADR-0011)
├── docs/                     # مستندات تکمیلی، نیازمندی‌ها و راهنماها
└── tests/                    # تست‌های واحد و یکپارچگی (Bun Test)
```

---

## ۴. قوانین و استانداردهای اختصاصی برای AI Agent

هنگام توسعه و اعمال تغییرات روی پروژه، پایبندی به موارد زیر الزامی است:

1. **بررسی قبل از اعمال تغییر (Read Before Edit):** قبل از هرگونه تغییر روی فایل‌ها، محتوای دقیق فعلی باید خوانده شود.
2. **رعایت سقف خطوط:** هرگز فایلی نسازید که از ۳۰۰ خط فراتر برود. در صورت نیاز به افزودن قابلیت، ماژول‌های فرعی ایجاد کرده و آنها را ایمپورت کنید.
3. **عدم افزودن سرور یا دیتابیس بدون درخواست صریح:** این برنامه به عنوان یک استودیوی کلاینت ایزوله طراحی شده است. هیچ API سروری نباید برای ذخیره‌سازی فایل‌ها ایجاد شود.
4. **اعتبارسنجی کیفی خودکار (Lint & Typecheck):** بعد از انجام هر تغییر، باید از طریق ابزارهای بررسی یا اجرای `npm run lint` و `npm run typecheck` از نبود خطای سینتکس، واردات نادرست و خطای تایپ اطمینان حاصل شود.
5. **حفظ استانداردهای دیزاین سیستم:** از اضافه کردن گرادیان‌های غیرمتعارف، رنگ‌های پیش‌بینی‌نشده، یا سایه‌های نامتعارف خودداری کنید. به قوانین `DESIGN.md` وفادار بمانید.
6. **پشتیبانی هم‌زمان دوزبانه (i18n):** هر رشته جدید در رابط کاربری باید در `constants/translations.ts` برای هر دو زبان `fa` و `en` تعریف شود.

---

## ۵. ماتریس تصمیمات معماری (ADR Guide)

| شناسه | عنوان | تصمیم کلیدی |
| :--- | :--- | :--- |
| **ADR-0001** | معماری RTL Markdown | جداسازی بلوک‌های کد LTR از بدنه و تراز جداول بدون تخریب سینتکس |
| **ADR-0002** | اسکرول نرم دسکتاپ | ارزیابی تنبل و فعال‌سازی Lenis صرفاً در پلتفرم‌های مجهز به ماوس دقیق |
| **ADR-0003** | تایپوگرافی هیبریدی کد | ترکیب JetBrains Mono و Vazirmatn جهت نمایش همگون عبارات فارسی درون کد |
| **ADR-0004** | هدر کپسولی شناور | طراحی جزیره شناور با افکت شیشه‌ای و مقاومت در برابر تکانه‌های اسکرول |
| **ADR-0005** | درز عمیق موتور پردازش | ایزوله‌سازی کامل `markdownEngine` از لایه ویو جهت تست‌پذیری مستقل |
| **ADR-0006** | سیستم موشن سه‌لایه‌ای | لایه‌های اولیه، بازخورد و محیطی مبتنی بر Spring Physics با پشتیبانی Reduced Motion |
| **ADR-0007** | داک شناور بزرگ‌نمایی | نوار ابزار متحرک مبتنی بر مجاورت نشانگر ماوس مشابه تجربه macOS |
| **ADR-0008** | حریم خصوصی بدون بک‌اند | پردازش ۱۰۰٪ محلی درون‌حافظه‌ای بدون لاگ، کوکی ردیابی یا سرور |
| **ADR-0009** | رهگیری خط فعال ادیتور | فرمول محاسبه خط فعال بدون خواندن مکرر ابعاد DOM جهت تضمین ۶۰fps |
| **ADR-0010** | خط‌لوله واکنشی معوق | جداسازی رندر ورودی متن و محاسبات سنگین سند با `useDeferredValue` |
| **ADR-0011** | دوزبانگی با صفر وابستگی | پیاده‌سازی فرهنگ لغت Type-safe بدون کتابخانه‌های حجیم جانبی |
| **ADR-0012** | کش LRU و پردازش غیرهمزمان | سیستم کش دوگانه کراندار در انجین و هایلایتر به همراه کارگر غیرمسدودکننده |
| **ADR-0013** | استقرار ایستا روی GitHub Pages | ساخت پکیج ایستا و پایپ‌لاین خودکار GitHub Actions با پشتیبانی ساب‌پث |

---

## ۶. چک‌لیست اضافه کردن ویژگی جدید (Feature Addition Checklist)

- [ ] آیا ویژگی پیشنهادی در محدوده کلاینت و حافظه محلی قرار دارد؟
- [ ] آیا حجم فایل جدید کمتر از ۲۰۰ تا ۳۰۰ خط است؟
- [ ] آیا مستندات JSDoc برای توابع و تایپ‌های اکسپورت شده درج شده است؟
- [ ] آیا کلیدهای متنی لازم به `constants/translations.ts` اضافه شده‌اند؟
- [ ] آیا وضعیت واکنش‌گرایی در نمایشگرهای موبایل و دسکتاپ چک شده است؟
- [ ] آیا تایپ‌ها در پوشه `types/` به صورت صریح تعریف شده‌اند؟
- [ ] آیا `npm run typecheck` و `npm run lint` با موفقیت پاس می‌شوند؟
