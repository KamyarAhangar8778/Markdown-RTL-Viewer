# سند معماری سیستم (System Architecture)

این سند تشریح‌کننده معماری فنی، جریان داده و تفکیک لایه‌های پروژه **RTL Markdown Studio** است.

---

## ۱. نمای کلی معماری (Architecture Overview)

معماری این سیستم بر پایه مدل **واکنشی تک‌صفحه‌ای سمت کلاینت (Client-Side Reactive SPA)** طراحی شده است. تمام پردازش‌های تبدیل متن، ایزولاسیون بلاک‌های کد و استخراج شاخص‌های آماری به صورت ۱۰۰٪ کلاینت‌محور (In-Memory) و بدون وابستگی به سرور انجام می‌پذیرد.

```
[ ورودی کاربر / بارگذاری فایل ]
              │
              ▼
[ لایه مدیریت وضعیت (MarkdownContext) ]
   ├─── ذخیره متن خام
   ├─── تنظیمات تبدیل
   └─── وضعیت‌های رابط کاربر
              │
              ├──────────────────────────────────┐
              ▼                                  ▼
[ پایپ‌لاین پردازش متن (Transformation) ]   [ موتور تحلیل آمار (Stats Engine) ]
   ├── ۱. تفکیک و ایزولاسیون کدها (LTR)         ├── شمارش کلمات و کاراکترها
   ├── ۲. تبدیل ارقام به فارسی (Persianizer)     ├── تفکیک کاراکترهای فارسی و لاتین
   └── ۳. تزریق کانتینر جهت‌دهی (RTL Injection)  └── محاسبه زمان تخمینی مطالعه
              │                                  │
              ▼                                  ▼
[ کامپایلر Markdown (ReactMarkdown + GFM) ]  [ نوار وضعیت (StatsBar) ]
              │
              ▼
[ نمایشگر نهایی پیش‌نمایش (PreviewView) ]
```

---

## ۲. تفکیک لایه‌ها و مسئولیت‌ها (Layered Architecture)

### ۲.۱. لایه پردازش و تبدیل متن (Transformation Pipeline)
- **موقعیت:** `utils/rtlConverter.ts` و `utils/persianizer.ts`
- **مسئولیت:** دریافت رشته مارک‌داون خام و اعمال پایپ‌لاین سه‌مرحله‌ای:
  1. **ایزولاسیون بلاک‌های کد (Code Block Tokenization):** جداسازی تگ‌های ` ``` ` و اینلاین‌کدها جهت جلوگیری از تغییر کاراکترها و اعداد داخل کد.
  2. **بومی‌سازی ارقام (Digit Persianization):** تبدیل اعداد انگلیسی به معادل فارسی در بدنه متن بدون اثرگذاری بر کدهای ساختاری مارک‌داون.
  3. **تزریق لفاف جهت‌دار (Directional Wrapper Injection):** بسته‌بندی متون در لایه‌های نگهدارنده با ویژگی `dir="rtl"` و ایزوله‌سازی بخش‌های ذاتا چپ‌به‌راست.

### ۲.۲. لایه مدیریت حالت سراسری (State Management Layer)
- **موقعیت:** `store/MarkdownContext.tsx`
- **مسئولیت:** مدیریت چرخه حیات داده با الگوی تک‌منبع حقیقت (Single Source of Truth):
  - ثبت متن خام (`rawMarkdown`) و متن پردازش‌شده (`convertedMarkdown`)
  - ذخیره گزینه‌های فعال کاربر (مانند تبدیل ارقام، بسته‌بندی در کانتینر RTL)
  - انتشار تغییرات به کامپوننت‌های مصرف‌کننده بدون ایجاد Re-render سربار

### ۲.۳. لایه هوک‌ها و منطق تعاملی (Custom Hooks Layer)
- **موقعیت:** `hooks/`
- **مسئولیت:** انتزاع رویدادهای جانبی و تعاملات مرورگر:
  - مدیریت دسترسی امن به کلیپ‌بورد و بازخورد کپی (`useClipboard`)
  - تشخیص مشخصات دستگاه کاربر جهت تنظیم رفتارهای تعاملی (`use-mobile`)
  - هماهنگ‌سازی پویای اسکرول بدون اختلال در عملکرد دیوایس‌های لمسی (`useDesktopSmoothScroll`)

### ۲.۴. لایه رندر و کامپایل Markdown (Rendering Engine)
- **موقعیت:** `components/views/preview-view.tsx`
- **مسئولیت:** تبدیل رشته مارک‌داون غنی‌شده به درخت المان‌های React (AST) با استفاده از `react-markdown` و پلاگین جدول‌های استاندارد `remark-gfm` با نگاشت امن المان‌های HTML.

---

## ۳. تضمین امنیت داده و کارایی (Security & Performance)

1. **حریم خصوصی بدون نشت داده (Zero-Data Exfiltration):** کل فرآیند تبدیل، پردازش فایل و کپی در حافظه محلی مرورگر اجرا شده و هیچ درخواستی حاوی محتوای متنی کاربر به شبکه‌های خارجی ارسال نمی‌گردد.
2. **جداسازی کامل نحو (Syntax Preservation):** قوانین تبدیل متن به گونه‌ای تعریف شده‌اند که سینتکس ساختاری مارک‌داون (مانند لینک‌ها، تصاویر، جداول و نقل‌قول‌ها) بدون شکستگی حفظ شود.
3. **پایداری در متون حجیم:** پردازش متن به صورت خطی با مرتبه زمانی O(n) روی طول رشته انجام می‌شود تا در اسناد طولانی تاخیر در ویرایش احساس نشود.
