'use client';

/**
 * @file components/ui/loader.tsx
 * @description UI adapter re-exporting the motion loader component and ASCII line spinner.
 */

export {
  Loader,
  AsciiLineLoader,
  ASCII_SETS,
} from '@/components/motion/loader';

export type {
  LoaderProps,
  LoaderVariant,
} from '@/components/motion/loader-types';
