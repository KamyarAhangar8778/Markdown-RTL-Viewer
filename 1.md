# گزارش تحلیل ماژولارسیون و معماری سورس‌کد (Architecture & Modularization Report)

## 1. Executive Summary

تحلیل جامع سورس‌کد مخزن **RTL Markdown Studio** نشان می‌دهد که این پروژه از ساختار اولیه‌ی تمیز، خوانا و تایپ‌سیف برخوردار است. با این حال، با اعمال معیارهای سخت‌گیرانه مهندسی نرم‌افزار، تفکیک وظایف (Single Responsibility)، کاهش Context Window هوش مصنوعی و رعایت سقف خطوط تعیین‌شده (۱۰۰ الی ۱۵۰ خط)، نقاط بهینه‌سازی مشخصی در کامپوننت‌های تعاملی و ابزارهای کمکی شناسایی شد.

- **تعداد کل فایل‌های سورس/کد:** ۳۹ فایل
- **فایل‌های بالای ۱۵۰ خط:** ۳ فایل (شامل ۲ کامپوننت React و ۱ فایل CSS)
- **وضعیت Modularization:** معماری کلی ماژولار است، اما در برخی بخش‌ها (مانند ترکیب داده‌های ایستا با توابع کمکی، یا ترکیب منطق تعریف اکشن‌های تعاملی با کانتینرهای بصری) مسئولیت‌های موازی وجود دارد که تفکیک آن‌ها فهم کد و هزینه توکن را برای AI بهینه‌تر می‌کند.

---

## 2. Large Files Inventory

در این جدول، تمامی فایل‌های بالای ۱۵۰ خط و فایل‌های کلیدی پرمسئولیت پروژه بر اساس اولویت ریفکتور مرتب شده‌اند:

| Priority | File | Total LOC | Code LOC | Classes | Functions | Responsibilities | Complexity | AI Context Cost | Refactor Benefit |
|---|---|---:|---:|---:|---:|---:|---|---|---|
| **High** | `components/layout/studio-dock.tsx` | 169 | 150 | 0 | 5 | 3 | Medium | Medium (~650 tokens) | High |
| **Medium** | `components/ui/magnification-dock.tsx` | 163 | 140 | 0 | 2 | 2 | Moderate | Medium (~600 tokens) | Medium |
| **Low** | `app/globals.css` | 206 | 169 | 0 | 0 | 4 | Low | Low-Medium (~550 tokens) | Low |
| **High** *(<150 but Multi-Resp)* | `utils/fileHelpers.ts` | 112 | 72 | 0 | 2 | 2 | Low | Low (~450 tokens) | High |
| **Medium** *(<150 but Context Hub)* | `store/MarkdownContext.tsx` | 115 | 94 | 0 | 5 | 3 | Low-Medium | Low-Medium (~500 tokens) | Medium |

### دسته‌بندی بر اساس بازه خطوط (Line Ranges):
- **بازه 151–250 خط:** ۳ فایل (`app/globals.css` [206], `components/layout/studio-dock.tsx` [169], `components/ui/magnification-dock.tsx` [163])
- **بازه 251–400 خط:** ۰ فایل
- **بازه 401–600 خط:** ۰ فایل
- **بازه 601+ خط:** ۰ فایل

---

## 3. Large but Cohesive

فایل‌هایی که اندازه نسبتاً بالایی دارند اما به دلیل ماهیت منسجم و تک‌مسئولیتی، تفکیک آن‌ها ارزش معماری پایینی دارد یا باعث شکستگی و Fragmentation غیرضروری می‌شود:

### 1. `app/globals.css` (۲۰۶ خط)
- **دلیل عدم پیشنهاد تفکیک فوری:** این فایل استایل‌های پایه Tailwind CSS v4، استایل‌های اختصاصی راست‌چین‌سازی خروجی مارک‌داون (`.markdown-rtl-content`)، قوانین اسکرول Lenis و کلاس‌های افکت شیشه‌ای را تجمیع کرده است. در ساختار فعلی Tailwind v4، یکپارچه بودن ورودی استایل سراسری بهینه است و تفکیک آن به چند فایل CSS جز ایجاد ایمپورت‌های مکرر مزیت عملکردی برای لاجیک AI ندارد.

---

## 4. High-Value Modularization Candidates

کاندیداهایی که بیشترین ارزش را برای ماژولارسازی، افزایش تمرکز AI، کاهش مصرف توکن و بهبود ساختار پروژه دارند:

### ۱. `components/layout/studio-dock.tsx` (اولویت: High)
- **تحلیل ساختاری:**
  - مسیر: `components/layout/studio-dock.tsx`
  - زبان/فریم‌ورک: TypeScript / React 19 Client Component
  - تعداد توابع: ۵ تابع (`handlePaste`، `handleDownload`، `togglePersianDigits`، `togglePreserveCode` و بدنه کامپوننت)
  - تعداد ایمپورت‌ها: ۸ ایمپورت مختلف (شامل ۶ آیکون Lucide، دو هوک، یک فایل هلپر و تایپ‌ها)
  - **مشکل معماری و چندمسئولیتی:** این کامپوننت همزمان وظیفه استخراج استیت‌ها از Context، تعریف لاجیک هندلرها و Toast، تولید ساختار آرایه ۹ آیتم Dock همراه با شرایط فعال‌بودن (Active States) و در نهایت رندر کانتینر بصری را بر عهده دارد.
  - **تأثیر روی AI:** هوش مصنوعی هنگام نیاز به تغییر دکمه‌ها یا لاجیک دانلود/پیست، کل کانتینر و ایمپورت‌های بصری را لود و تحلیل می‌کند.

### ۲. `utils/fileHelpers.ts` (اولویت: High)
- **تحلیل ساختاری:**
  - مسیر: `utils/fileHelpers.ts`
  - زبان: TypeScript Pure Utilities
  - تعداد توابع: ۲ تابع (`downloadFile`, `readTextFile`) + یک ثابت داده‌ای بزرگ (`SAMPLE_TEMPLATES`)
  - **مشکل معماری:** ترکیب توابع خالص File I/O مرورگر با داده‌های متنی حجیم قالب‌های نمونه مارک‌داون فارسی (`SAMPLE_TEMPLATES` با بیش از ۶۰ خط متن خام).
  - **تأثیر روی AI:** هربار که ایجنت نیاز به فراخوانی یا بررسی توابع دانلودی/خواندنی فایل دارد، حجم زیادی از متن قالب‌های نمونه را به Context تحمیل می‌کند.

### ۳. `components/ui/magnification-dock.tsx` (اولویت: Medium)
- **تحلیل ساختاری:**
  - مسیر: `components/ui/magnification-dock.tsx`
  - زبان/فریم‌ورک: TypeScript / React Motion Physics UI
  - تعداد توابع: ۲ تابع (`DockItem` و `MagnificationDock`)
  - **مشکل معماری:** تعریف کامپوننت فرعی داخلی `DockItem` با تمام محاسبات فیزیک انیمیشن موس و Tooltip در کنار کامپوننت اصلی نوار Dock.
  - **تأثیر روی AI:** تفکیک `DockItem` باعث می‌شود هر فایل به زیر ۱۰۰ خط کاهش یابد و لاجیک محاسباتی فنری هر المان مستقل شود.

---

## 5. Recommended Split Structure

### ساختار پیشنهادی ۱: `components/layout/studio-dock.tsx`

```text
Current File:
components/layout/studio-dock.tsx (169 LOC)
        ↓
Suggested Structure:
hooks/useStudioDockActions.ts   ← استخراج لاجیک هندلرها و تولید آیتم‌های Dock
components/layout/studio-dock.tsx ← صرفاً کانتینر متصل‌کننده و رندر Dock
        ↓
Responsibilities:
- useStudioDockActions.ts: مدیریت استیت‌های Context، عملیات کلیپ‌بورد، دانلود فایل، تولید لیست DockItemData
- studio-dock.tsx: مدیریت چیدمان و مانت کردن MagnificationDock در پایین صفحه
        ↓
Dependencies:
studio-dock.tsx → useStudioDockActions.ts → (MarkdownContext, useClipboard, fileHelpers)
```

---

### ساختار پیشنهادی ۲: `utils/fileHelpers.ts`

```text
Current File:
utils/fileHelpers.ts (112 LOC)
        ↓
Suggested Structure:
constants/sampleTemplates.ts  ← انتقال آرایه داده‌های SAMPLE_TEMPLATES
utils/fileHelpers.ts          ← توابع خالص downloadFile و readTextFile
        ↓
Responsibilities:
- constants/sampleTemplates.ts: نگهداری قالب‌های آماده مارک‌داون فارسی و اطلاعات دمو
- utils/fileHelpers.ts: توابع خالص خواندن و ذخیره فایل بر بستر Web APIs
        ↓
Dependencies:
fileHelpers.ts: بدون وابستگی به داده‌های نمونه
MarkdownContext.tsx → constants/sampleTemplates.ts
```

---

### ساختار پیشنهادی ۳: `components/ui/magnification-dock.tsx`

```text
Current File:
components/ui/magnification-dock.tsx (163 LOC)
        ↓
Suggested Structure:
components/ui/dock/dock-item.tsx        ← کامپوننت محاسباتی و انیمیشنی تک‌آیتم و تول‌تیپ
components/ui/dock/magnification-dock.tsx ← کانتینر نوار ابزار و رهگیری مختصات موس
        ↓
Responsibilities:
- dock-item.tsx: محاسبه فاصله موس، transform مقیاس و انیمیشن با useSpring، نمایش تول‌تیپ
- magnification-dock.tsx: مدیریت MotionValueهای سراسری نوار و نگاشت آیتم‌ها
        ↓
Dependencies:
magnification-dock.tsx → dock-item.tsx
```

---

## 6. Dependency / Architecture Observations

```text
app/page.tsx
 ├── Header → HeaderBrand → useDecodeText
 ├── StudioDock → MagnificationDock
 │     ├── useClipboard
 │     ├── downloadFile
 │     └── MarkdownContext
 ├── EditorView → MarkdownContext
 ├── PreviewView → ReactMarkdown / remarkGfm → MarkdownContext
 └── UploadModal → readTextFile → MarkdownContext

MarkdownContext (State Hub)
 ├── rtlConverter → persianizer
 ├── statsCalculator
 └── fileHelpers (SAMPLE_TEMPLATES)
```

### مشاهدات کلیدی معماری:
1. **عدم وجود Circular Dependency:** دیاگرام وابستگی پروژه کاملاً جهت‌دار (DAG) و بدون چرخه است.
2. **Coupling در استیت سراسری:** `MarkdownContext` به درستی مسئولیت محاسبه مقادیر مشتق‌شده (`rtlMarkdown` و `stats`) را از طریق `useMemo` مدیریت می‌کند.
3. **تفکیک تمیز UI اتمیک:** کامپوننت‌های پوشه `components/ui/` کاملاً مستقل هستند و به لایه‌های بالاتر وابستگی معکوس ندارند.

---

## 7. AI Context Impact

| فایل | مصرف تقریبی توکن | سختی Navigation برای AI | پیچیدگی استدلال (Reasoning) | منفعت تفکیک برای AI |
|---|---|---|---|---|
| `components/layout/studio-dock.tsx` | ~650 Tokens | Medium | Medium | **High** (جداسازی لاجیک از JSX مانع سردرگمی ایجنت در ویرایش دکمه‌ها می‌شود) |
| `utils/fileHelpers.ts` | ~450 Tokens | Low | Low | **High** (حذف ۶۰+ خط متن استاتیک مارک‌داون از لاجیک کمکی فایل‌ها) |
| `components/ui/magnification-dock.tsx` | ~600 Tokens | Medium | High | **Medium** (کاهش پیچیدگی خوانش فرمول‌های فیزیک انیمیشن) |
| `store/MarkdownContext.tsx` | ~500 Tokens | Low | Low-Medium | **Medium** (جداسازی استیت Toastها از مارک‌داون در آینده) |

---

## 8. Project Statistics

- **Total Source / Code Files:** ۳۹ فایل
- **Files > 150 LOC:** ۳ فایل
- **Files > 250 LOC:** ۰ فایل
- **Files > 400 LOC:** ۰ فایل
- **Files > 600 LOC:** ۰ فایل
- **Largest Source File:** `app/globals.css` (۲۰۶ خط) / `components/layout/studio-dock.tsx` (۱۶۹ خط)
- **Average Source File Size:** ~۵۸ خط در هر فایل
- **Very High Priority Files:** ۰ فایل
- **High Priority Files:** ۲ فایل (`components/layout/studio-dock.tsx`, `utils/fileHelpers.ts`)
- **Medium Priority Files:** ۲ فایل (`components/ui/magnification-dock.tsx`, `store/MarkdownContext.tsx`)
- **Low Priority Files:** ۱ فایل (`app/globals.css`)

---

## 9. Final Priority List

```text
1. components/layout/studio-dock.tsx — High
2. utils/fileHelpers.ts — High
3. components/ui/magnification-dock.tsx — Medium
4. store/MarkdownContext.tsx — Medium
5. app/globals.css — Low
```
