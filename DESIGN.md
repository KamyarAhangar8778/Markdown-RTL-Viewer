# سیستم طراحی و مشخصات بصری (Design System Specification)

این سند مشخصات فنی و استانداردهای طراحی رابط کاربری **RTL Markdown Studio** را بر پایه معماری Dark Glassmorphism، تایپوگرافی استاندارد فارسی و رنگ‌بندی با کنتراست بالا تشریح می‌کند.

---

## ۱. هویت بصری و فلسفه طراحی (Design Philosophy)

- **تم کلی (Theme):** Dark Cinematic Glassmorphism (شیشه‌ای تیره با کنتراست بالا)
- **جهت‌گیری محوری (Directionality):** معماری کامل RTL (راست‌چین) با تفکیک هوشمند بلاک‌های کد و جداول LTR
- **پایداری بصری:** حذف گرادیان‌های شلوغ و اتکا به ترکیب دقیق نور، سایه و بافت مینیمال

---

## ۲. پالت رنگی (Color System)

| متغیر / المان | کد رنگی / مقدار CSS | کاربرد |
| :--- | :--- | :--- |
| **پس‌زمینه اصلی (Canvas)** | `#000000` | لایه زیرین سراسری برنامه |
| **پنل‌های شیشه‌ای (Glass Panels)** | `rgba(12, 12, 15, 0.85)` | هدر چسبان، نوار ابزار، کادر ادیتور و پیش‌نمایش |
| **کارت‌ها (Glass Cards)** | `rgba(20, 20, 24, 0.70)` | کارت‌های آماری، مودال‌ها و بخش‌های متمرکز |
| **فیلدهای ورودی (Inputs)** | `rgba(10, 10, 12, 0.95)` | تکست‌اریا، اینپوت‌ها و دراپ‌داون‌ها |
| **حاشیه پیش‌فرض (Subtle Border)** | `rgba(255, 255, 255, 0.08)` | تفکیک ظریف لایه‌ها بدون ایجاد خطوط تیز |
| **حاشیه فوکوس (Focus Border)** | `rgba(255, 255, 255, 0.30)` | وضعیت فوکوس و تعاملات فعال |
| **متن اصلی (Primary Text)** | `#fafafa` (Zinc 50) | تیترها، محتوای متنی اصلی و برچسب‌های مهم |
| **متن ثانویه (Secondary Text)** | `#a1a1aa` (Zinc 400) | توضیحات فرعی، آمار و راهنماها |
| **رنگ آکسان زمردی (Emerald)** | `#10b981` | نشانگرهای وضعیت موفقیت، دانلود و خروجی |

---

## ۳. تایپوگرافی و فونت‌ها (Typography)

- **فونت اصلی فارسی:** `Vazirmatn` (لود شده از طریق `next/font/google` با قابلیت کش پایدار و سلف‌هاست، بدون دانلود مجدد و تاخیر شبکه)
- **فونت بلاک‌های کد و قطعه‌کدها (Monospace Hybrid):** ترکیب هوشمند `JetBrains Mono` (برای واژگان، عملگرها و نمادهای انگلیسی) و بازگشت خودکار به `Vazirmatn` (برای کاراکترها و کامنت‌های فارسی درون کد) با فعال‌سازی فونت‌لیگچرهای برنامه‌نویسی.
- **استراتژی کش فونت‌ها (Font Caching):** کش نامحدود مرورگر (`immutable caching`) با بهینه‌سازی Subsetting و Preload در لایه App Router.
- **ارتفاع خطوط (Line-Height):** `1.85` برای متون فارسی در پیش‌نمایش، `1.6` برای المان‌های کنترلی
- **مقیاس اندازه قلم (Type Scale):**
  - **H1:** `1.85rem` / وزن ۷۰۰ / حاشیه زیرین ملایم
  - **H2:** `1.50rem` / وزن ۷۰۰
  - **H3:** `1.25rem` / وزن ۶۰۰
  - **متن بدنه (Body):** `0.875rem` تا `1rem`

---

## ۴. افکت‌ها و متریال (Effects & Materials)

- **بافت نویز پس‌زمینه (`bg-noise`):** لایه الگو SVG با شفافیت ۲٪ برای عمق‌بخشی بدون کاهش کنتراست.
- **تاری پس‌زمینه (Backdrop Filter):** بلور ۱۲ تا ۱۶ پیکسل برای پنل‌ها و مودال‌ها (`backdrop-blur-xl` / `backdrop-blur-md`).
- **سایه‌های نوری (Glow & Depth):** `0 10px 30px -5px rgba(0, 0, 0, 0.8)` همراه با هایلایت داخلی `inset 0 1px 0 0 rgba(255, 255, 255, 0.05)`.
- **اسکرول‌بار اختصاصی:** عرض ۶ پیکسل با دستگیره شیشه‌ای مینیمال (`rgba(255, 255, 255, 0.18)`).

---

## ۵. استانداردهای انیمیشن و تعامل (Micro-Interactions)

- **افکت رمزگشایی متن (Decoding Effect):** انیمیشن تایپ سایبری در عنوان اصلی هدر برنامه.
- **ترنزیشن‌ها:** `transition-all duration-200 ease-in-out` روی دکمه‌ها و المان‌های تعاملی.
- **هدر چسبان (Sticky Header):** تثبیت در بالای صفحه با پس‌زمینه تیره نیمه‌شفاف و افکت شیشه‌ای جهت دسترسی سریع به ابزارها.
