# سیستم طراحی

این سند مشخصات و استانداردهای طراحی رابط کاربری **RTL Markdown Studio** را تشریح می‌کند.

---

## ۱. هویت بصری

- **تم کلی:** رابط کاربری تیره و شیشه‌ای (Dark Glass) با پشتیبانی کامل از تم روشن
- **جهت‌گیری محوری:** ساختار راست‌چین (RTL) همراه با تفکیک بلاک‌های کد و جداول LTR
- **کنتراست:** استفاده از طیف خاکستری استاندارد Zinc با کنتراست مناسب متون

---

## ۲. پالت رنگی

| المان | مقدار رنگی | کاربرد |
| :--- | :--- | :--- |
| **پس‌زمینه اصلی (Canvas)** | `#000000` | لایه زیرین برنامه در حالت تیره |
| **پنل‌های شیشه‌ای** | `rgba(12, 12, 15, 0.85)` | هدر، نوار ابزار و کادر ویرایشگر |
| **کارت‌ها** | `rgba(20, 20, 24, 0.70)` | مودال‌ها و بخش‌های آماری |
| **ورودی‌ها** | `rgba(10, 10, 12, 0.95)` | کادر ورود متن |
| **حاشیه پیش‌فرض** | `rgba(255, 255, 255, 0.08)` | تفکیک ظریف المان‌ها |
| **متن اصلی** | `#fafafa` (Zinc 50) | عناوین و متن اصلی |
| **متن ثانویه** | `#a1a1aa` (Zinc 400) | توضیحات فرعی و آمار |
| **رنگ شاخص** | `#10b981` (Emerald) | نشانگرهای وضعیت موفقیت |

---

## ۳. تایپوگرافی

- **فونت فارسی:** `Vazirmatn`
- **فونت کد:** `JetBrains Mono` با بازگشت به `Vazirmatn` برای کاراکترهای فارسی درون کد
- **ارتفاع خطوط (Line-Height):** `1.85` برای متون فارسی در پیش‌نمایش و `1.6` برای المان‌های کنترلی
- **مقیاس اندازه متن:**
  - **H1:** `1.85rem` با وزن ۷۰۰
  - **H2:** `1.50rem` با وزن ۷۰۰
  - **H3:** `1.25rem` با وزن ۶۰۰
  - **متن بدنه:** `0.875rem` تا `1rem`

---

## ۴. تعاملات و سیستم موشن (Motion Design System)

سیستم انیمیشن و موشن دیزاین برنامه بر اساس اصول مهندسی حرکت و الگوهای تخصصی موشن تدوین شده است:

### ۴.۱. شخصیت موشن (Motion Personality Archetype)
- **آرکتایپ انتخابی:** استودیو مهندسی / ابزار دقیق (Corporate / Premium Studio)
- **منحنی‌های سرعت:**
  - **ورود (Decelerate):** `cubic-bezier(0.16, 1, 0.3, 1)` برای فرود آرام و کنترل‌شده
  - **خروج (Accelerate):** `cubic-bezier(0.7, 0, 0.84, 0)` برای خروج سریع و بی‌معطلی
  - **انتقال درون‌صفحه‌ای:** `cubic-bezier(0.65, 0, 0.35, 1)`
- **پالت زمانی (Duration Palette):**
  - **آنی (Instant - 80ms):** بازخورد لمسی، کلیک‌ها و ابزارک‌ها (<100ms)
  - **سریع (Quick - 150ms):** تغییر آیکون‌ها، تعویض برچسب‌ها و نشان‌ها
  - **استاندارد (Standard - 250ms):** جابجایی کارت‌ها، تب‌ها و کانتینرها
  - **کند (Slow - 380ms):** باز شدن مودال‌ها، منوها و پنل تمرکز (Focus Mode)
  - **نسبت خروج (Exit Ratio):** ۷۰٪ زمان ورود (خروج‌ها همیشه ۳۰٪ سریع‌تر از ورود هستند)

### ۴.۲. لایه‌های سه‌گانه انیمیشن (Three Motion Layers)
1. **لایه اولیه (Primary Layer):** اکشن اصلی (مانند ورود مودال با اسکیل ۹۴٪ به ۱۰۰٪، اسلاید نرم توست یا جابجایی کانتینر ویوها)
2. **لایه ثانویه (Secondary Layer):** بازخورد لمسی و ارتعاش (فشردن دکمه با مقیاس ۰.۹۷، پاپ تایید چک‌مارک، یا لرزش خطای توست با نوسان ۱۰px±)
3. **لایه محیطی (Ambient Layer):** حیات پیوسته و تنفس ملایم نوار وضعیت (نوسان سینوسی ۲.۵ ثانیه‌ای در نشانگر موتور و شناوری لطیف وضعیت خالی پیش‌نمایش)

### ۴.۳. پیکربندی فنرها (Physics Springs)
- **SPRING_PRESS:** `{ stiffness: 480, damping: 28, mass: 0.5 }` — فشردن و رهایش فوری دکمه‌ها
- **SPRING_PANEL:** `{ stiffness: 360, damping: 32, mass: 0.6 }` — فرود طبیعی مودال‌ها و دیالوگ‌ها
- **SPRING_LAYOUT:** `{ stiffness: 340, damping: 28, mass: 0.6 }` — لغزش نشانگر فعال داک بین حالت‌ها
- **SPRING_DOCK:** `{ stiffness: 280, damping: 18, mass: 0.1 }` — بزرگ‌نمایی شناور داک به سبک macOS
- **SPRING_BOUNCE:** `{ stiffness: 320, damping: 16, mass: 0.5 }` — جهش کنترل‌شده تایید موفقیت و کپی

### ۴.۴. دسترس‌پذیری (Accessibility & Reduced Motion)
- رعایت خودکار تنظیمات سیستم‌عامل با `useReducedMotion()`. در صورت فعال بودن ترجیح کاهش حرکت، انیمیشن‌های جابجایی و اسکیل خاموش شده و تنها ترنزیشن‌های رنگ و محوشدگی فوری اعمال می‌شوند.

