# راهنمای توسعه و قوانین مهندسی هوش مصنوعی (AI Agent Guidelines)

این سند مرجع اصلی راهنمایی، استانداردها و قوانین مهندسی برای عامل‌های هوش مصنوعی (AI Agents) در پروژه **RTL Markdown Studio** است. تمامی اصلاحات و کدهای جدید باید بدون استثنا از این اصول پیروی کنند.

---

## ۱. ساختار پشته فنی (Technology Stack)

- **فریم‌ورک اصلی:** Next.js 15 (App Router) + React 19
- **زبان و تایپ‌سیفتی:** TypeScript (Strict Mode)
- **مدیریت استیت:** React Context API (`store/MarkdownContext.tsx`)
- **رندرینگ و پردازش Markdown:** `react-markdown` + `remark-gfm` با ایزولاسیون بلاک‌های کد
- **طراحی و استایل‌دهی:** به فایل `DESIGN.md` ارجاع شود (از تعریف مجدد متغیرها و مشخصات بصری در این فایل خودداری کنید).

---

## ۲. قوانین ساختاری و اندازه فایل‌ها (Separation of Concerns & Modularity)

1. **سقف حجم فایل‌ها:** هیچ فایلی نباید از ۱۰۰ الی ۱۵۰ خط فراتر برود. در صورت افزایش حجم، کدها باید به کامپوننت‌ها، هوک‌ها یا هلپرهای کوچک‌تر تفکیک شوند.
2. **اصل تک‌مسئولیتی (Single Responsibility Principle - SRP):** هر فایل، کامپوننت، هوک یا تابع تنها باید یک وظیفه مشخص و معین را انجام دهد.
3. **تفکیک لایه‌های پروژه:**
   - منطق پردازشی و محاسباتی خالص درون پوشه `utils/`
   - مدیریت استیت و اکشن‌ها درون پوشه `store/`
   - تعامل با رابط کاربر و منطق‌های بازاستفاده‌پذیر درون پوشه `hooks/`
   - تایپ‌های سراسری و اینترفیس‌ها درون پوشه `types/`
   - مستندات معماری در `adr/` و راهنماهای فنی در `docs/`

---

## ۳. استاندارد مستندسازی و JSDoc (Documentation Standards)

- **الزام JSDoc:** تمام توابع محاسباتی، تبدیل‌کننده‌های متن (RTL/Persianizer)، هوک‌های سفارشی و متدهای پیچیده باید دارای مستندات کامل JSDoc شامل شرح عملکرد، پارامترها (`@param`)، خروجی (`@returns`) و رفتار در شرایط مرزی باشند.
- **توضیحات درون‌کدی:** توضیحات باید تمیز، فنی، مختصر و بدون حشو و اضافات باشند.

---

## ۴. ساختار دایرکتوری‌ها (Directory Structure)

```text
/
├── adr/               # رکوردهای تصمیمات معماری (Architecture Decision Records)
├── app/               # صفحات و ساختار App Router در Next.js
├── components/
│   ├── layout/        # کامپوننت‌های ساختاری هدر، فوتر، سایدبار و اسکرول
│   ├── ui/            # المان‌های پایه و اتمیک (Button, Card, Modal, Tooltip, ...)
│   └── views/         # ماژول‌ها و نماهای اصلی (Editor, Preview, StatsBar, UploadModal)
├── docs/              # مستندات فنی، دیاگرام‌ها و راهنمای توسعه
├── hooks/             # هوک‌های سفارشی React (Clipboard, Decoding, SmoothScroll, ...)
├── lib/               # توابع کمکی و ابزارهای مشترک
├── store/             # ارائه‌دهنده و مدیریت استیت سراسری (Context Provider)
├── tests/             # تست‌های واحد (Unit) و یکپارچگی (Integration)
├── types/             # تعاریف تایپ‌ها و اینترفیس‌های TypeScript
└── utils/             # توابع خالص تبدیل متن، آمارگیری و کار با فایل‌ها
```

---

## ۵. تضمین کیفیت و اعتبارسنجی کد (Quality Assurance & Validation)

- **بررسی صحت کد پس از تغییرات:** پس از انجام هرگونه ویرایش، حتماً بررسی خطاهای سینتکس، تطابق با استانداردها و اجرای ابزارهای اعتبارسنجی (Linting / Compilation) باید انجام شود.
- **ایزولاسیون جهت متن:** اطمینان از اعمال `dir="rtl"` برای متون فارسی و ایزولاسیون کامل `dir="ltr"` برای قطعه‌کدها، تگ‌های `pre`، `code` و جداول انگلیسی.

---

## ۶. اصول پاسخگویی و رفتار ایجنت (Agent Behavioral Guidelines)

1. **پاسخ‌های دقیق و متمرکز:** توضیحات اضافی و حاشیه‌ای را حذف کرده و روی نتیجه مستقیم کار تمرکز کنید.
2. **صداقت و دقت مهندسی:** از بزرگ‌نمایی و ادعاهای غیرواقعی خودداری کنید.
3. **شفاف‌سازی قبل از اقدام:** در صورت وجود ابهام در درخواست، قبل از اعمال تغییرات از کاربر سؤال بپرسید.

