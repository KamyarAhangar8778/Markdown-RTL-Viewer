پروژه باید ساختار `Scalable` و `حرفه‌ای` داشته باشه.
مستندسازی (Documentation): یک برنامه نویس حرفه‌ای برای کدهایش مستندات می‌نویسد.
JSDoc: برای توابع پیچیده، توضیحات استاندارد بنویسید.
سیستم طراحی اختصاصی (Design System).
بعد از اتمام هر تغیرات در پروژه، چک کردن `لینتینگ، و فرمتینگ و Syntax چکینگ` و ... رو انجام بده .
قوانین حجم فایل و تفکیک وظایف (Separation of Concerns):
حداکثر خطوط هر فایل: سعی کنید هیچ فایلی بیشتر از `200` تا `300` خط نشود.
قانون تک‌مسئولیتی (Single Responsibility):باید هر فایل یک کاری رو انجام بده.
ساختار فایل ها و پوشه های پروژه باید `خوانا، حرفه‌ای و ماژولار` باشد تا `AI Agent` وقتی دارد روی پروژه کار میکند `گیج` نشود.
`زیاد` به من `توضیح` نده، `خلاصه` بگو و `تمرکزت` روی `کار اصلی` باشه.
دروغ نگو، `صادق باش` اغراغ نکن.
حرف هایی که یک بار زدی رو `دوباره تکرار` نکن.
هر جایی که فکر کردی منظور من رو نفهمیدی ازم قبل از این که کاری انجام بدی ازم سوال هایه شفاف کننده بپرس تا من جوابتو بدم.
برای ریز-فریم‌ورک‌های `Micro-Frontends (معماری ماژولار)` از `Module Federation` استفاده کن.
حتما در شروع اول چت فایل `ARCHITECTURE.md` و `DESIGN.md` رو بخوان.
داخل مسیر `.gemini/skills` من برای تو `SKILL` های مهمی گذاشتم که تو باید قبل از انجام هر کاری بری و ببینی چه `SKILL` هایی مرتبط با کاری که انجام میدهی هستش و آن ها را بخوانی و بعد شروع به توسعه کنی.
کار ها رو موازی همزمان انجام بدی تا سریع تر بریم جلو مثله استفاده موازی از چند ابزار یا خواندن موازی چندین فایل یا ادیت همزمان چند فایل.
در بهنیه سازی و ریفکتور فایل ها باید بهم یک حدود درصد واقی از افزایش سرعت و ... داشتیم نسبت به قبل بهینه سازی و ریفکتور ... .
الگوریتم های هوشمندانه باید بنویسیم این خیلی خوبه. 